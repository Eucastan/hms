apiVersion: v1
kind: Service
metadata:
  labels:
    app: hms-auth
  name: hms-auth
spec:
  ports:
  - name: http
    port: 4000
    protocol: TCP
    targetPort: 4000
  selector:
    app: hms-auth
  type: ClusterIP

